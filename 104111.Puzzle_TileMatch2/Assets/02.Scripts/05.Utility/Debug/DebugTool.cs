#if UNITY_EDITOR

using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public partial class DebugTool : MonoBehaviour
{
/*    public Text windowText;

    private const string formatWindow = "<color=green>Pre : {0}</color>\n<color=yellow>Cur : {1}</color>";
    private UIManager uiManager { get { return UIManager.Instance; } }

    private void Update()
    {
        windowText.text = string.Format(formatWindow, uiManager.prevWindow, uiManager.currentWindow);
    }*/
}

#endif